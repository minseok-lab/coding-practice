class Solution {
    public int solution(int num1, int num2) {
        int answer = 0;
        if (num1 == num2) { //만약 num1과 num2가 같다면
            return answer = 1; //1 출력
        }
        else return answer = -1; //그 외의 경우, -1 출력
    }
}